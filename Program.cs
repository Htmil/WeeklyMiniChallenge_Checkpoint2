using WeeklyMiniChallenge_Checkpoint2;

var productList = new List<Product>(); // Had to create the list here i couldnt figure out where to put it in my methods. 

MyMethods.CreateProducts(productList);
MyMethods.DisplayProducts(productList);
MyMethods.DisplayTotalSum(productList);